<%= form_for(@voxeljson) do |f| %>
  <% if @voxeljson.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voxeljson.errors.count, "error") %> prohibited this voxeljson from being saved:</h2>

      <ul>
      <% @voxeljson.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :json %><br>
    <%= f.text_area :json %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
